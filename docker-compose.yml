services:
  nim-proxy:
    build: .
    image: nim-proxy:latest
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file: .env
    read_only: true
    cap_drop: [ALL]
    security_opt:
      - no-new-privileges:true
